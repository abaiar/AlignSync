# AlignSync 车轮定位仪协同制造与溯源平台 - 项目详细说明书

## 1. 项目背景与概述

本项目旨在设计并开发一个专供**核心技术企业**与**定位仪生产厂**协同使用的车轮定位仪生产制造与供应链管理系统。

一台完整的车轮定位仪由定位仪软件（受软件锁授权控制）、若干相机、目标靶以及横梁、立柱、机柜等机械结构组成。其生产过程具有高度协同性：核心技术企业负责提供底层核心部件（软件与已标定的相机），定位仪生产厂负责采购核心部件并与机械结构组装成整机销售。

本系统通过建立严密的数字资产管理、B2B 订单流转和基于硬件特征的防伪授权机制，实现跨企业的高效协同、防伪管控及全链路追溯。

---

## 2. 系统参与方与角色划分

系统基于多租户架构，主要分为两大业务阵营，由**权限管理子系统**进行严格的企业及人员授权与访问控制。

### 2.1 核心技术企业 (平台方)
* **相机生产人员**：负责组装、标定相机，并将参数同步至系统。
* **软件管理员**：负责软件锁的功能授权与使用授权录入。
* **业务人员**：负责处理生产厂的采购订单、核对库存并完成扫码发货。
* **财务人员**：负责核对订单打款记录并确认收款。
* **质量人员**：负责追溯设备生产及售后信息。

### 2.2 定位仪生产厂 (租户方)
* **采购员**：负责向技术企业发起采购、上传付款凭证、确认收货。
* **组装人员**：负责将硬件组装成整机，在系统中进行 BOM 绑定并登记设备档案。

---

## 3. 核心业务与功能模块

1.  **资产数字化管控**：相机（编号与标定数据）与软件锁（功能版本与授权）的入库与溯源。
2.  **供应链协同流转**：标准的 B2B 订单全生命周期管理（下单 -> 确认 -> 付款 -> 收款确认 -> 发货 -> 签收）。
3.  **设备组装与防伪授权**：将软件锁与特定设备（基于相机内参等标识信息）绑定，系统动态生成专属“授权号”，控制软件功能与使用期限。
4.  **售后逆向物流**：相机的返修、更换、退货；软件的升级、激活、更换和退货。
5.  **数据大盘与统计**：汇总并可视化一段时间内的采购、生产、发货、退货等的型号与数量。

---

## 4. 详细系统用例 (Use Cases)

### 4.1 核心资产入池模块

#### 用例 1：相机信息同步 (UC-CAM-001)
* **参与者**：核心技术企业相机生产人员
* **前置条件**：1. 相机已组装完成并完成标定；2. 人员已登录系统；3. 拥有“相机管理”权限。
* **后置条件**：相机信息保存到系统库存中，状态为“在库”。
* **主事件流**：
    1. 进入“相机管理”模块，点击“同步相机信息”按钮。
    2. 调用外部标定系统接口获取相机数据。
    3. 系统校验相机 SN 是否已存在。
    4. 系统保存相机信息（型号、SN、标定参数等）。
    5. 系统自动生成相机内部 ID 并显示“同步成功”提示。
* **业务规则**：相机 SN 全局唯一；标定数据必须完整（包含内参、外参）。

#### 用例 2：软件锁授权同步 (UC-SW-001)
* **参与者**：核心技术企业软件管理员
* **前置条件**：1. 软件锁已在授权系统中完成授权配置；2. 人员已登录系统。
* **后置条件**：软件锁授权信息保存到系统，状态为“已授权”。
* **主事件流**：
    1. 进入模块，点击“同步授权信息”。
    2. 扫描软件锁 ID，系统调用接口获取授权详情。
    3. 系统保存授权信息（版本、功能列表、到期日等）。
    4. 系统显示“授权同步成功”。

### 4.2 B2B 采购订单协同模块

#### 用例 3：提交采购订单 (UC-PO-001)
* **参与者**：定位仪生产厂采购员
* **主事件流**：选择产品型号与数量 -> 系统计算总额 -> 提交订单 -> 系统生成 PO 编号并通知业务员。

#### 用例 4：确认采购订单 (UC-PO-002)
* **参与者**：核心技术企业业务员
* **主事件流**：核对订单与库存 -> 填写意见 -> 确认订单 -> 通知生产厂付款。

#### 用例 5：订单付款 (UC-FIN-001)
* **参与者**：定位仪生产厂采购员
* **主事件流**：上传付款凭证（截图/回单） -> 填写付款详情 -> 提交审核。

#### 用例 6：收款确认 (UC-FIN-002)
* **参与者**：核心技术企业财务人员
* **主事件流**：核对银行到账 -> 点击确认收款 -> 通知业务员安排发货。

#### 用例 7：产品发货 (UC-SHIP-001)
* **参与者**：核心技术企业业务员
* **核心逻辑**：选取具体库位中的相机 SN 和软件锁 SN 绑定至订单 -> 输入物流单号 -> 确认发货。

#### 用例 8：收货确认 (UC-RCV-001)
* **参与者**：定位仪生产厂采购员
* **主事件流**：核对实物与系统清单 -> 确认收货 -> 资产进入生产厂本地库存。

### 4.3 生产制造与数据溯源模块

#### 用例 9：登记定位仪设备 (UC-ASM-001)
* **参与者**：定位仪生产厂组装人员
* **核心逻辑**：
    1. 输入整机 SN。
    2. 绑定 1 个已采购的软件锁。
    3. 绑定 N 个已采购的相机（指定左/右位置）。
    4. **系统生成基于相机内参的授权号**。
    5. 更新各部件状态为“已使用”。

#### 用例 10：查询设备追溯信息 (UC-QRY-001)
* **功能**：通过整机 SN 一键查询其包含的相机标定参数、软件锁版本、采购日期及生产人员。

---

## 5. 实施优先级与研发计划

| 优先级 | 用例编号 | 用例说明 | 备注 |
| :--- | :--- | :--- | :--- |
| **P0 (核心)** | UC-CAM-001 | 相机信息同步 | 基础数据源 |
| **P0 (核心)** | UC-SW-001 | 软件锁授权同步 | 授权管控基础 |
| **P0 (核心)** | UC-PO-001 | 提交采购订单 | 业务起点 |
| **P0 (核心)** | UC-ASM-001 | 登记定位仪设备 | 核心生产逻辑 |
| **P1 (重要)** | UC-PO-002 | 确认采购订单 | 协同流程 |
| **P1 (重要)** | UC-FIN-001 | 订单付款 | 资金流 |
| **P1 (重要)** | UC-FIN-002 | 收款确认 | 资金流 |
| **P1 (重要)** | UC-SHIP-001 | 产品发货 | 物流与资产交割 |
| **P2 (一般)** | UC-RCV-001 | 收货确认 | 闭环 |
| **P2 (一般)** | UC-QRY-001 | 查询设备追溯信息 | 增值与质控 |
