# 车轮定位仪生产协同管理系统 - 需求规格说明书 (PRD)

## 1. 项目概述

本系统旨在为“核心技术企业”与“定位仪生产厂”提供一个协同管理的平台，实现车轮定位仪从核心零部件（相机、软件锁）的生产、标定、采购、发货，到整机组装、授权验证及售后追溯的全生命周期闭环管理。

### 1.1 核心设备组成

一台完整的车轮定位仪主要包括以下关键部件：

* **定位仪软件**（通过软件锁授权管理，分不同功能版本）
* **若干相机**（含多种型号，需编号与标定）
* **若干目标靶**
* **其他机械结构**（横梁、立柱、机柜等，由生产厂自行配备或组装）

### 1.2 核心业务逻辑

* **核心技术企业**：负责相机组装与标定、软件开发及软件锁授权。
* **定位仪生产厂**：向核心技术企业采购相机和软件锁，组装成整机对外销售。
* **授权机制**：软件锁使用设备前需进行授权。系统根据设备的标识信息（如相机内参）生成**授权号**，以控制软件锁对该设备的使用期限。同一软件锁用于不同设备时，需生成不同的授权号。

---

## 2. 全局模块需求

### 2.1 权限管理子系统

* **功能描述**：完成相关企业（核心技术企业、定位仪生产厂）及其内部人员的账号分配、角色定义、操作授权及数据可见性权限控制管理。

### 2.2 统计汇总模块

* **功能描述**：支持按时间段、产品型号、企业等多维度，汇总和分析采购量、生产量、发货量、退货量等核心业务数据。

### 2.3 其他售后业务支持

* **相机售后**：支持相机的返修、更换、退货流程及状态追踪。
* **软件售后**：支持软件的在线/离线升级、重新激活、更换及退货处理。

---

## 3. 系统用例详情

### UC-CAM-001：相机信息同步

* **参与者**：核心技术企业相机生产人员
* **前置条件**：1. 相机已组装完成并完成标定；2. 人员已登录系统；3. 拥有“相机管理”权限。
* **后置条件**：相机信息保存到系统库存中，状态为“在库”。
* **主事件流**：
1. 相机生产人员进入“相机管理”模块。
2. 点击“同步相机信息”按钮。
3. 调用外部标定系统接口获取相机数据。
4. 系统校验相机SN是否已存在。
5. 系统保存相机信息（型号、SN、标定参数等）。
6. 系统自动生成相机内部ID。
7. 系统显示“同步成功”提示。


* **扩展流**：
* **4a. 相机SN已存在**：系统提示“相机SN已存在，请检查” -> 人员确认是否为重复录入 -> 如重复则取消操作；如需更新则选择覆盖。


* **业务规则**：
* `BR-CAM-001`: 相机SN全局唯一。
* `BR-CAM-002`: 标定数据必须完整（包含内参、外参）。


* **非功能需求**：同步响应时间 < 3秒；支持批量同步（一次最多100个相机）。

### UC-SW-001：软件锁授权同步

* **参与者**：核心技术企业软件管理员
* **前置条件**：1. 软件锁已在授权系统中完成配置；2. 管理员已登录系统；3. 拥有“软件授权管理”权限。
* **后置条件**：软件锁授权信息保存到系统，状态为“已授权”。
* **主事件流**：
1. 软件管理员进入“软件授权管理”模块。
2. 点击“同步授权信息”按钮。
3. 输入或扫描软件锁ID。
4. 系统调用授权系统接口获取授权详情。
5. 系统校验软件锁ID是否已存在。
6. 系统保存授权信息（软件版本、功能列表、到期日等）。
7. 系统绑定授权给指定生产厂（如有）。
8. 系统显示“授权同步成功”。


* **扩展流**：
* **5a. 软件锁ID已存在**：系统提示“该软件锁已有授权记录” -> 管理员可选择：更新授权 / 取消。
* **6a. 授权信息不完整**：系统提示缺少必填字段 -> 管理员需完善授权信息后重试。


* **业务规则**：
* `BR-SW-001`: 一个软件锁同一时间只能授权给一个生产厂。
* `BR-SW-002`: 功能版本需与软件产品定义一致。



### UC-PO-001：提交采购订单

* **参与者**：定位仪生产厂采购员
* **前置条件**：1. 采购员已登录系统；2. 拥有“采购管理”权限；3. 已确定采购需求。
* **后置条件**：采购订单生成，状态为“待确认”。
* **主事件流**：
1. 进入“采购管理”模块，点击“新建采购订单”。
2. 选择订单类型（相机采购 / 软件采购）。
3. 从产品目录中选择型号/规格。
4. 输入采购数量，系统显示预期总金额。
5. 采购员确认订单信息并提交。
6. 系统生成唯一订单编号，并通知核心技术企业业务员。


* **扩展流**：
* **查询产品信息**：采购员可随时查看规格、库存量等详情。
* **库存不足**：系统提示当前库存量，采购员可调整数量或联系技术企业。
* **取消提交**：可保存为草稿，稍后提交。


* **业务规则**：
* `BR-PO-001`: 订单编号格式：PO + yyyyMMdd + 4位流水号。
* `BR-PO-002`: 软件采购时必须指定具体功能版本。



### UC-PO-002：确认采购订单（技术企业）

* **参与者**：核心技术企业业务员
* **前置条件**：存在“待确认”订单，业务员已登录且拥有权限。
* **后置条件**：订单状态变更为“已确认”，通知生产厂付款。
* **主事件流**：
1. 进入“订单管理”，查看待确认列表并进入详情。
2. 核对产品、数量、价格，确认库存是否满足。
3. 输入确认意见，点击“确认订单”。
4. 系统更新状态为“已确认”，通知生产厂采购员和财务。


* **扩展流**：
* **库存不足**：可部分确认（确认可发货数量）或拒绝订单并说明原因。
* **订单信息有误**：选择“驳回订单”并填写原因，系统通知生产厂修改。


* **业务规则**：
* `BR-PO-003`: 订单确认后不可修改，只能补充。
* `BR-PO-004`: 确认后需在3个工作日内完成发货。



### UC-FIN-001：订单付款

* **参与者**：定位仪生产厂采购员
* **前置条件**：订单为“已确认”状态。
* **后置条件**：生成付款记录，状态变更为“待收款确认”。
* **主事件流**：
1. 在“我的订单”中选择订单并点击“去付款”。
2. 选择付款方式，上传付款凭证（截图/回单）。
3. 输入付款金额、账号、日期并确认提交。
4. 系统生成付款记录，更新状态为“待收款确认”，通知技术企业财务。


* **扩展流**：
* **线下转账**：系统显示企业收款账户信息供打款参考。


* **业务规则**：
* `BR-FIN-001`: 付款金额必须等于订单总额。
* `BR-FIN-002`: 付款凭证必须清晰可见。



### UC-FIN-002：收款确认（技术企业财务）

* **参与者**：核心技术企业财务人员
* **前置条件**：订单为“待收款确认”状态。
* **后置条件**：状态变更为“已付款”，通知业务员发货。
* **主事件流**：
1. 进入“财务-收款管理”，查看付款记录与凭证。
2. 核对银行到账信息，确认后点击“确认收款”。
3. 系统更新状态为“已付款”，记录操作人/时间，通知业务员发货。


* **扩展流**：
* **未查到到账记录**：标记为“待核实”并备注原因，延迟确认。
* **金额不符**：标记“金额异常”，联系采购员核对，等待补充或退回。


* **业务规则**：
* `BR-FIN-003`: 收款确认后不得撤销。



### UC-SHIP-001：产品发货（技术企业）

* **参与者**：核心技术企业业务员
* **前置条件**：订单为“已付款”状态，且有对应库存。
* **后置条件**：状态变更为“已发货”，系统扣减库存。
* **主事件流**：
1. 在“待发货”列表中选择订单点击“发货”。
2. 选取具体产品（软件锁序列号/相机SN），系统自动匹配库存。
3. 输入物流信息（快递公司+单号），确认并提交。
4. 系统更新状态，扣减库存，并向生产厂发送通知。


* **扩展流**：
* **手动选取产品**：支持扫码枪录入或手动勾选。
* **部分发货**：支持分批发货，未发货部分后续处理。


* **业务规则**：
* `BR-SHIP-001`: 软件锁发货时必须记录目标生产厂。
* `BR-SHIP-002`: 发货单号必须填写。



### UC-RCV-001：收货确认（生产厂）

* **参与者**：定位仪生产厂采购员
* **前置条件**：订单为“已发货”状态，已实际收到货物。
* **后置条件**：状态变更为“已完成”，产品状态变为“已收货/在库”。
* **主事件流**：
1. 在“待收货”列表中核对产品清单。
2. 上传收货照片/签收单，点击“确认收货”。
3. 系统更新订单为“已完成”，更新产品库存状态。


* **扩展流**：
* **差异/损坏**：填写差异说明并上传证据，系统通知技术企业处理。


* **业务规则**：
* `BR-RCV-001`: 收货确认后7天内可发起退换货申请。



### UC-ASM-001：登记定位仪设备（生产组装）

* **参与者**：定位仪生产厂组装人员
* **前置条件**：已收到部件产品，具备“生产组装”权限。
* **后置条件**：生成定位仪设备档案，实现部件追溯。
* **主事件流**：
1. 进入“生产组装”，点击“新增设备登记”。
2. 输入设备基础信息（型号、名称）并生成/输入唯一设备SN。
3. 扫描/选择已采购的软件锁，系统校验授权状态。
4. 扫描/选择所需相机，并为其指定位置编号（如：左相机、右相机）。
5. 系统自动生成BOM结构，确认后保存并生成设备ID。
6. 更新相关软件锁和相机的状态为“已使用”。


* **扩展流**：
* **软件锁已过期**：系统阻断操作并提示，需联系采购更新授权。
* **相机状态异常**：如已标记使用，系统提示冲突需人工核实。


* **业务规则**：
* `BR-ASM-001`: 一台定位仪至少使用2个相机。
* `BR-ASM-002`: 设备SN全局唯一。
* `BR-ASM-003`: 一个软件锁只能绑定一台定位仪。



### UC-QRY-001：查询设备追溯信息

* **参与者**：生产厂组装人员、技术企业质量人员
* **前置条件**：用户已登录并拥有查询权限。
* **后置条件**：显示完整的设备生产追溯链条。
* **主事件流**：
1. 进入“设备追溯”页面，输入查询条件（设备SN / 相机SN / 软件锁ID）。
2. 系统返回完整追溯信息，包括：设备基本信息、软件锁详情（含授权期）、相机明细（含标定参数）、关联采购订单、组装人及日期。
3. 用户可选择导出追溯报告。


* **业务规则**：
* `BR-QRY-001`: 追溯信息保留时间不少于10年。



---

## 4. 用例优先级与实现顺序

下表规划了本系统各业务用例的开发优先级与功能定位，建议按优先级倒序逐步迭代开发：

| 优先级 | 用例编号 | 用例名称 | 业务说明 |
| --- | --- | --- | --- |
| **P0（核心）** | `UC-CAM-001` | 相机信息同步 | 基础数据来源 |
| **P0（核心）** | `UC-SW-001` | 软件锁授权同步 | 核心部件管理 |
| **P0（核心）** | `UC-PO-001` | 提交采购订单 | 业务流程起点 |
| **P0（核心）** | `UC-ASM-001` | 登记定位仪设备 | 最终产出物管理 |
| **P1（重要）** | `UC-PO-002` | 确认采购订单 | 协同流程核心 |
| **P1（重要）** | `UC-FIN-001` | 订单付款 | 业务资金流 |
| **P1（重要）** | `UC-FIN-002` | 收款确认 | 业务资金流验证 |
| **P1（重要）** | `UC-SHIP-001` | 产品发货 | 实体物流追踪 |
| **P2（一般）** | `UC-RCV-001` | 收货确认 | 流程闭环确认 |
| **P2（一般）** | `UC-QRY-001` | 查询设备追溯信息 | 质量管理增值功能 |